import { Checkbox } from "~components/checkbox-card"

import "~style.css"

function IndexPopup() {
  return (
    <div className="flex justify-center">
      <Checkbox text="hello world"/>
      <Checkbox text="welcome"/>
    </div>
  )
}

export default IndexPopup
